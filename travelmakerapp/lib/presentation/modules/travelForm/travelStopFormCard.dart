import 'package:flutter/material.dart';

class TravelStopFormCard extends StatelessWidget {
  const TravelStopFormCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Card(
      child: Column(
        children: [
          // where goes the map and the experienses
          Stack(
            children: [

            ],
          ),
          Row(
            children: [
              Text(''),
            ],
          )
        ],
      ),
    );
  }
}
